Sources for the plugin `plugin.appodeal.beta.Mobvista`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Mobvista" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
